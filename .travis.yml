sudo: required
dist: trusty
language: node_js
node_js:
- '8'
branches:
  only:
  - master
cache:
  directories:
  - node_modules

install:
  - npm install
script:
  - npm run lint
  - npm run test
deploy:
  provider: script
  skip_cleanup: true
  script:
  - npm run build && npx semantic-release
